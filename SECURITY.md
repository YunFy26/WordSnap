# WordSnap 安全策略

## 支持范围

WordSnap 仍处于早期开发阶段。安全修复仅进入最新发布版本和 `main` 分支，旧版本不单独维护。

| 版本 | 是否支持 |
| --- | --- |
| 最新发布版本 | 是 |
| `main` 分支 | 是 |
| 旧发布版本 | 否 |

## 报告安全漏洞

不得在公开 Issue、讨论区或 Pull Request 中披露漏洞细节、API Key、真实选中文本或可直接利用的复现数据。

1. 在仓库的 Security 页面使用 Report a vulnerability 功能提交私密报告。
2. 如果该功能暂时不可用，请创建一个不含漏洞细节的普通 Issue，仅说明需要与维护者建立私密联系。
3. 报告中应提供受影响版本、运行平台、影响范围、最小复现步骤和可能的缓解措施。所有密钥与个人数据必须脱敏。

维护者将在收到报告后确认接收状态，随后评估影响、准备修复并协调披露时间。由于项目仍处于早期阶段，目前不承诺固定响应时限。可能泄露 API Key、选中文本、剪贴板内容或本地词表的数据问题将被优先处理。

## 重要安全说明

- 选中文本会发送至用户配置的 OpenAI 兼容服务。相关隐私政策和数据保留规则由服务提供方决定。
- API Key 当前保存在应用数据目录的 `settings.json` 中，不使用系统密钥库。Unix 平台会尝试将文件权限限制为仅当前用户可读写。
- 连接远程服务时应使用 HTTPS。HTTP 可能导致 API Key 和选中文本以明文形式通过网络传输。
- 应用通过模拟复制读取选区，并尝试恢复原剪贴板。剪贴板恢复失败或数据丢失应作为安全或隐私问题处理。
- 应用将每次运行的分级日志分别保存在项目根目录的 `logs/` 文件夹中。日志不得包含 API Key、选中文本、译文、剪贴板内容、完整服务响应或用户配置的接口地址。Unix 平台会尝试将日志文件权限限制为 `0600`。

完整数据流和本地数据清理方法见[隐私与数据处理说明](docs/PRIVACY.md)。
